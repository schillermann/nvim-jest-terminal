# Jest Terminal For Neovim

The idea is to put the jest command with the appropriate parameters into the terminal, for manual execution.

Builds jest test command for single test execution, with the test description under the cursor.

```sh
:JestSingleInTerminal
```

Build jest test command, to execute all tests in the test file.

```sh
:JestFileInTerminal
```
